package com.entecmedia.boendersfitness.models;

import com.entecmedia.boendersfitness.models.daos.AllOpeningHoursDao;
import com.entecmedia.boendersfitness.models.daos.HomeFragmentDao;
import com.entecmedia.boendersfitness.models.daos.InformationFragmentDao;
import com.entecmedia.boendersfitness.models.daos.OpeningHoursDao;

import java.util.ArrayList;

// Class yet to be implemented

/*

public class Repository {

    private ArrayList<String> homeFragmentDescriptionTexts;
    private ArrayList<String> specialOpeningHoursNames;
    private ArrayList<String> specialOpeningHoursTimes;

    private HomeFragmentDao homeFragmentDao;
    private OpeningHoursDao openingHoursDao;
    private AllOpeningHoursDao allOpeningHoursDao;
    private InformationFragmentDao informationFragmentDao;

    public boolean isSameLength;

    public void getDataSetHomeFragment(){

        homeFragmentDescriptionTexts.set(value);
        homeFragmentDescriptionTexts.set(value);
        homeFragmentDescriptionTexts.set(value);
        homeFragmentDescriptionTexts.set(value);

    }

    public void getSpecialOpeningHours (){

        isSameLength = false;

        if (specialOpeningHoursNamesVanuitCache.getSize() == specialOpeningHoursNamesVanuitCache.getSize()) {

            isSameLength = true;

        } else if (specialOpeningHoursNames.size() == 0 || specialOpeningHoursTimes.getSize()) {

            // for some reason, either one or both the special opening hour / holiday arrays are inspected as empty

        }

        else {

            // for some reason, a special opening time / holiday has a name, but specified time, or the other way around

        }

    }

}

*/
